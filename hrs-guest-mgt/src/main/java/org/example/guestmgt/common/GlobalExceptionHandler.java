package org.example.guestmgt.common;

import org.springframework.web.bind.annotation.RestControllerAdvice;

@RestControllerAdvice
public class GlobalExceptionHandler {
    // TODO: A class that globally handles runtime exceptions and returns corresponding http response
    // e.g. Data validation errors
}
